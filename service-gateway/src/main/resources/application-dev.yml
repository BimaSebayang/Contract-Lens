server:
  port: 9001

spring:
  application:
    name: SERVICE_GATEWAY

  datasource:
    url: jdbc:postgresql://localhost:5432/contractlens_gateway
    username: sa
    password: sa
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: false
        jdbc:
          time_zone: UTC
          lob.non_contextual_creation: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  data:
    mongodb:
      host: localhost
      port: 27017
      database: contractlens
      username: contractlens
      password: contractlens123
      authentication-database: admin

    redis:
      host: localhost
      port: 7000
      password:
      timeout: 5000ms
      database: 0

      lettuce:
        pool:
          enabled: true

  rabbitmq:
    host: localhost
    port: 5672
    username: contractlens_user
    password: contractlens_pass
    virtual-host: /